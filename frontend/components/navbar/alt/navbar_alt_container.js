import { connect } from 'react-redux';
import NavBarAlt from './navbar_alt';

const mapStateToProps = (state) => {
  return {

  };
};

export default connect(
  mapStateToProps,
  null
)(NavBarAlt);
